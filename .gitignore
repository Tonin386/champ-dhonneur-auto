# Python
__pycache__/
*.pyc
.pytest_cache/
build/
*.egg-info/

# Caches de dépendances (remplis une fois hors ligne : make wheels, make debs, cargo vendor)
wheels/
debs/
rust/vendor/
rust/target/
web/node_modules/
web/tsconfig.tsbuildinfo

# Spectateur compilé (make front)
champ_dhonneur/server/web/

# Données produites : entraînements, parties, modèles
# (sauf le meilleur modèle de chaque entraînement, suivi par Git : commits faits à la main)
runs/**
!runs/**/
!runs/*/modeles/meilleur.pt
archives/
data/
modeles/*.pt

# historique des parties de la page « Jouer » (CHAMP_PARTIES)
parties/
